#include <iostream>
// #include <array>
#include <vector>
void prims(std::vector<std::vector<int>> graph)
{
    int n = graph.size();
    std::vector<int> parent(n);
    std::vector<int> cost(n);

    // checking if the edge is already included in the mst or not
    std::vector<bool> ver(n, false);
}

int main()
{
}